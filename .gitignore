__pycache__/
*.py[cod]

# Snapshots — ~45 MB of binary parquet each, and they are point-in-time market data that
# no diff can usefully describe. They stay on disk; scoring replays them via --replay.
market_screener/data/raw/
